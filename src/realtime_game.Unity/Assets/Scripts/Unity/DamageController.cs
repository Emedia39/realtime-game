using UnityEngine;

public class DamageController : MonoBehaviour
{
    /*
    //スクリプト
    //[SerializeField] HpRodController hpRodControle;//あれ…
    //体力
    public float maxHp = 100f;//HpRodControle.maxHP
    public float currentHp;//HpRodControle.currentHP

    void Start()
    {
        currentHp = maxHp;
    }

    public void TakeDamage(float damage)
    {
        currentHp -= damage;
        Debug.Log($"{gameObject.name} に {damage} ダメージ");

        if (currentHp <= 0)
        {
            KnockDown();
        }
    }

    void KnockDown()
    {
        Debug.Log($"{gameObject.name} は倒された");
        gameObject.SetActive(false);
    }
    */
}
